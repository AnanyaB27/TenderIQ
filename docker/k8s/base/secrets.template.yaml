# Placeholder structure only. Real secret values are never committed —
# they are injected at deploy time via the cluster's secret store
# (docs/architecture/Architecture.md §13.5).
apiVersion: v1
kind: Secret
metadata:
  name: backend-api-secrets
  namespace: backend
type: Opaque
stringData:
  DB_URL: ""
  REDIS_URL: ""
  JWT_SECRET: ""
  RAZORPAY_API_KEY: ""
