import { Injectable } from '@nestjs/common';

/**
 * Placeholder — real Passport JWT strategy wiring (secret, extraction,
 * validate()) is implemented in a later phase.
 */
@Injectable()
export class JwtStrategy {}
